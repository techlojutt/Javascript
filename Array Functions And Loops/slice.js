// it does not affect on the original array but it returns the cutting array.
var listOfStudentNames = ["Student1","Student2","Student3","Student4"];
var returnResult = listOfStudentNames.slice(0,2)
console.log(returnResult);